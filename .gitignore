venv/
.venv/
__pycache__/
*.pyc

# Tiene la connection string de Supabase, con la contrasenia adentro.
.env

# La base ya no se versiona: los datos viven en Supabase (una sola base para
# todos). Antes db.sqlite3 estaba EN el repo y era el metodo de sincronizacion,
# lo que traia un conflicto binario irresoluble cada vez que dos personas
# cargaban datos.
db.sqlite3
db.sqlite3.backup-*
*.sqlite3-journal
